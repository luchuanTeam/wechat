<view class='head-tab-container'>
  <view class='menu-wrapper'>
    <view class='head-tab-menu'>
      <view wx:for='{{menuList}}' wx:key='{{index}}' class='text {{ index == activeTab ? "active" : "" }}
      bindtap='menuTap' data-index='{{index}}'>
        {{item}}
      </view>
    </view>
  </view>
</view>