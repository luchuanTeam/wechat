<view class='exam-container'>
  <view class='exam-wrapper' wx:for='{{examList}}' wx:key='{{index}}'>
    <view class='exam' bindtap='toCatalog' data-desc='{{item.desc}}'>{{item.desc}}</view>
  </view>
</view>
